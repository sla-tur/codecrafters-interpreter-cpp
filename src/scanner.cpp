#include <vector>

class Scanner {
    private:
        const std::tring source;
        const std::vector<token> tokens = new ;
        int start{0};
        int current{0};
        int line{1};

    public:
        Scanner(std::string source) {
            
        }
}