{
    "configurations": [
      {
        "type": "cppdbg",
        "request": "launch",
        "name": "Launch Program",
        "program": "${workspaceFolder}/a.out",
        "args": [
          "tokenize",
          "${input:filename}"
        ],
        "preLaunchTask": "build"
      }
    ],
    "inputs": [
      {
        "type": "promptString",
        "id": "filename",
        "description": "Enter the filename to process"
      }
    ]
  }